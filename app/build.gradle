plugins {
    id 'com.android.application'
}

android {
    namespace 'com.mokano.mokatube'
    compileSdk 35

    defaultConfig {
        applicationId "com.mokano.mokatube"
        minSdk 23
        targetSdk 28
        versionCode 3
        versionName "0.3"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
